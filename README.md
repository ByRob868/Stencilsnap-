# StencilSnap

Upload an image → instantly preview a purple tattoo stencil with a before/after slider.
No generate button. Live sliders for tuning.

## Backend (FastAPI)
```bash
cd backend
python -m venv .venv
# Windows: .venv\Scripts\activate
# Mac/Linux: source .venv/bin/activate
pip install -r requirements.txt
python -m app.main
